# Disponibilites — libellés des créneaux corrigés

Les 4 créneaux restent sur une seule ligne.
Les libellés sont centrés et ne sont plus coupés par `overflow:hidden`.

Conserver pin.js et config.js existants.
